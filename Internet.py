#!/usr/bin/python3
import internet. datacenter. lastmile. backbone()
import IP address.IPv6()
import DNS. ("How it works")
import Domain name ("what is the domain name")
import hosting ("what is hosting")
